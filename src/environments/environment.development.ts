const BaseApi = 'https://highland-coffee.onrender.com/api';
export const environment = {
    production: false,
    product: `${BaseApi}/products`,
    customer: `${BaseApi}/customers`,
    auth: `${BaseApi}/auth`,
    category: `${BaseApi}/categories`,
};
